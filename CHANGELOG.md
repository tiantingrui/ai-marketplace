# 变更日志

本项目遵循语义化版本。公开版本的重要变化记录在这里。

## 未发布

### 新增

- 增加随插件交付的配置、规则、影响、评审上下文、评审输出及规则注册表 Schema。
- 增加五类内置确定性规则的机器可读注册表和仓库级 `AGENTS.md`。
- Marketplace 校验器支持遍历多个本地插件和动态 Skill 目录。

### 变更

- Git revision 在每次分析开始时解析为固定 commit OID；CLI 对未知、重复、空值和缺值参数执行严格校验。
- 评审输出验证器对 finding、文本字段和建议数组执行稳定类型检查。
- 对 `.ai-marketplace.json` 的嵌套字段、枚举、仓库相对路径、重复规则 ID 和风险域引用执行严格校验。
- CI 验证矩阵覆盖 Ubuntu、macOS 以及 Node.js 20、22、24。

### 安全

- 敏感路径在内容级 diff 前过滤，并对 Marketplace 声明路径执行符号链接和真实路径边界检查。
- 拒绝读取未跟踪符号链接、仓库外真实路径、二进制文件和超过输入上限的普通文本文件。
- 配置文件必须是仓库根目录中的常规文件，不能通过符号链接加载仓库外内容。

## 1.0.0 - 2026-07-20

### 新增

- 发布 `ai-marketplace` 与 `frontend-engineering-standard` 的通用公开身份。
- 提供项目规则自检、需求影响分析和 PR 智能评审三个 Skill。
- 支持目标仓库通过 `.ai-marketplace.json` 配置路径规则、导入规则、应用关系与风险建议。
- 将只读执行引擎完整打包进插件，确保远端安装后可直接运行。
- 增加公共配置示例、治理文档、安全策略、自动测试与发布门禁。
- 采用 Apache License 2.0 作为公开发行许可证。

### 安全

- 默认过滤环境文件、密钥、证书、签名材料与常见凭据路径。
- 所有首发能力保持只读，模型评审不作为唯一合并依据。
