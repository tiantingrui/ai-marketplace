# 安全策略

## 支持范围

安全修复优先覆盖最新稳定标签。旧版本可能只提供升级建议。

## 报告漏洞

请通过 GitHub 仓库的 Security Advisory 私下报告漏洞，不要在公开 Issue、PR、日志或聊天中粘贴密钥、Token、用户数据或可直接利用的细节。

报告建议包含：

- 受影响版本与能力。
- 最小复现步骤和实际影响。
- 是否需要特殊权限或用户交互。
- 已知缓解措施。

维护者确认问题后会评估严重程度、准备修复与回归测试，并在安全发布完成后公开必要说明。

## 默认安全边界

- 首发插件只读分析本地 Git 仓库。
- 不自动修改代码、发布评论、批准或合并 PR。
- 不执行部署、回滚、生产写入或用户数据操作。
- 过滤环境文件、私钥、证书、签名文件和常见凭据路径。
- AI 输出仅作辅助证据，不能替代 CI 与人工审批。

实现细节见 [`docs/security.md`](docs/security.md)。
